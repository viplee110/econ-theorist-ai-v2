# Consumable Quality Certificates - V5.3 public framing pilot

Use `$econ-theorist-v2` in this exact selected root to initialize a public
theory project named **Consumable Quality Certificates**. Continue through
each engine-selected, agent-authorized pre-G1 framing route, one canonical
commit at a time. Stop as soon as the engine requires a human-owned G1
decision, or earlier if a route reaches a terminal failure. Do not make any
human-owned structural decision.

## Research seed

An infinite sequence of short-lived buyers chooses between two long-lived
sellers. In each period a seller privately observes whether its service
capacity is high or low. A high-capacity seller may pay cost `k` for a truthful
public certificate. A certificate remains visible until one buyer trades with
that seller, at which point it is consumed. An uncertified seller may be high
or low. Before choosing, a buyer can pay search cost `c` to inspect one
uncertified seller's current capacity, can buy without inspecting, or can exit.
A successful service gives the buyer value one; sellers earn a margin from a
successful trade and discount future profits.

Frame, without solving, whether lowering certificate cost `k` can make buyer
search, allocative efficiency, or buyer surplus non-monotone because the
endogenous stock and depletion of certificates changes the composition of the
uncertified pool and the return to inspection. State an exact falsifiable
question, why its answer matters economically, a kill condition, and exact
benchmarks that separate the consumable-stock channel from mechanical cheaper
certification and from endogenous buyer inspection. Do not claim a direction,
novelty, proof, validation, or welfare result.

## Execution constraints

- Treat this file as the only researcher-authored scientific seed.
- Read the local skill completely and use only the installed
  `etai codex invoke` bridge.
- Invoke every bridge request through the local
  `capture_codex_invocation.py` helper, with unique stdout, stderr, and
  metadata paths beneath `run/` and `.host-state` as its isolated state root.
- Treat each returned WorkPacket and its candidate authoring contract as the
  sole engine instruction, scientific context, and mechanical schema authority.
- Complete only the currently authorized route. Continue solely when a
  canonical committed response authorizes another agent route.
- Produce one candidate at a time. If the bridge returns structured repair
  diagnostics, make at most two sequential repairs using only those diagnostics.
- Save exact requests and responses, every candidate attempt, timestamps, and
  a concise agent report in this root or the packet-declared shadow root.
- Use no web search, literature, reference answer, source checkout, repository
  history, tests, fixtures, gold cases, old outputs, parent/sibling directories,
  or other agent context.
- Do not spawn subagents.
- Stop before any human gate. Never fabricate or infer human confirmation.
