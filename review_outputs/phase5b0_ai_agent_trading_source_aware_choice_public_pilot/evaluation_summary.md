# Phase 5B AI-agent-trading source-aware choice pilot evaluation

## Archive role

This is an additive postflight evaluation of the fresh public pilot at:

`C:\tmp\etai-v2-ai-agent-trading-source-aware-20260724-2a81a4c`

The pilot is frozen at `awaiting_user_choice`. Do not apply a user choice,
create a worker handoff, call `finish`, author or complete a framing candidate,
or treat either displayed direction as a stable research dependency.

This evaluation does not overwrite the operator's `agent-report.md`. It
corrects that report's inaccurate input-fidelity statement and separates a
successful bounded protocol slice from contaminated scientific evidence.

## Evidence binding

- engine commit represented by the wheel:
  `2a81a4c049d0af523cea6447f8e159bdfe7a56cb`
- wheel SHA-256:
  `3e4e6431c3f910887343a3c843a7f00af3d0c88d2697fd50eebd4500602d3f8c`
- pilot manifest SHA-256:
  `8862a4d80f6e6249c208605e76c3e17473999c42a18885df23a64d8ec1a0a8c1`
- task prompt SHA-256:
  `cc33fcd04091b07383c03c9df8fc8d1264e3141348d057d16b0fd55d3130c47e`
- WorkPacket hash:
  `d88aebf95385f4b80568edacc02967c6f8d3bc0cfeeff46503a3309a3898d52f`
- delivery envelope hash:
  `ced1d4b29ac975985431f97b42c6f734ed5ad83cc20d72bba3ae15eeb23acf6c`
- team plan hash:
  `3fc455498c69e2a697f932d85e32bea25de7670d4b6a5a8f559f34ea0a0d643a`
- panel hash:
  `41e14191d693babe1fde520ffbe6ac5ee316b29486832a956552d17a3e529166`
- choice-review hash:
  `a012b816c77c81bf5b1bda261fb260fa569cceb4e42121ac252f103030989fe5`
- final canonical head:
  `0ad5bacf1daf52245971e7ad072405da8ad1cfd5180e05001977ee2d023e48d1`
- observable model label: `GPT-5`; provider and backend were not
  independently established.

The per-file paths, byte counts, and digests are in
[`evidence_binding.json`](evidence_binding.json).

## Separate verdicts

| Dimension | Verdict | Bound conclusion |
|---|---|---|
| Machine/protocol | **PASS — bounded slice only** | The immutable panel and review are mutually bound and the bridge stopped at `awaiting_user_choice`. Failed calls reported `mutated: false`; no later authority boundary was crossed. |
| Input fidelity | **FAIL** | The task prompt contains the correct UTF-8 Chinese brief, but the request and delivered WorkPacket contain mojibake. |
| Scientific framing | **MIXED / contaminated** | Both cards are concrete and falsifiable, but their common provenance/source-lineage premise was influenced by a host-only label absent from the researcher's brief. Both also have high literature-overlap risk. |
| Team diversity | **MIXED** | A studies correlated information and implementability in an asset market; B studies false-name cloning and source-level payment in forecast procurement. Their strategic objects differ, but both remain in one provenance family. |
| Researcher UX | **MIXED** | Complete cards repair the earlier one-line-summary problem, but the visible return was still hash/report-led, the input was corrupted, the full payload exceeded 100 KB, and recovery required expert Windows path knowledge. |
| Clean first use | **NOT ESTABLISHED** | Eventual protocol recovery is not a clean ordinary-path first-use pass. |
| Research quality / novelty | **NOT ESTABLISHED** | The source review is an orientation only; it establishes neither novelty, formal validity, economic-interpretation validity, nor a paper-worthy question. |

## Input-fidelity failure

The intended brief in `TASK_PROMPT.md` is:

> 经济问题：研究 AI agents 互相 trading 的问题，以及 agent trading 下的 mechanism design 问题。

Its UTF-8 byte count is 113 and its SHA-256 is:

`44dcd82fd024169240e8c6d4f7e7141a072c2af929874f94fc60b224703cb054`

The actual `requested_scope` and `framing_intent` in
`run\02-start-request.json` begin with `缁忔祹闂...`. The actual value has
143 UTF-8 bytes and SHA-256:

`e22b3ec1c9e52f22d11478a47fb44ac911d7737231bb5d10eeb0e291f3399220`

The bridge faithfully retained those already-corrupted request values in
`WorkPacket.run_input`; it did not create the corruption. The failure occurred
at the prompt-to-request host boundary. Therefore the operator report's claim
that the request preserved the brief “byte-for-byte” is false.

The exact responsible host conversion is not established by this evidence.
Future operation must compare the JSON-decoded request values with the exact
user turn before invocation and compare the delivered `run_input` again before
any scientific lane is opened.

## Host-label scientific contamination

The researcher did not propose source awareness, provenance, or source
lineage. The host nevertheless initialized the project as:

`AI Agent Trading Source-Aware Framing Pilot`

That title entered the canonical Project entity and the WorkPacket's compiled
scientific context. The mentor then explicitly began its narrowing with “If
source-aware means ...”, collaborator A proposed an authenticated source-
overlap indicator, and collaborator B proposed verifiable source identifiers.

This is direct evidence that a transport/evaluation label affected the blind
scientific panel. It does not prove that the model independently discovered
provenance as the best primitive for the user's broad AI-agent-trading
question. The later source search occurred only after the panel was immutable
and therefore did not cause the initial convergence.

Future clean lanes must receive only the exact WorkPacket and their role
overlay, while project names and inherited host context remain scientifically
neutral. Capability names such as `source_aware_choice` describe a post-panel
literature-orientation transport and must never be interpreted as a research
primitive.

## Scientific interpretation

The new cards are materially better decision artifacts than the prior short
titles:

- A provides a matched source-blind/source-aware diagnostic with loss
  `117/676` versus `27/160`, but that inequality is presently only a standard
  value-of-information result. A paper-worthy increment would need a strict
  strategic implementability separation under explicit incentive,
  participation, and budget constraints, or a sharp equivalence theorem.
- B provides an exact cloning deviation: three independent sources have
  accuracy `27/32`; identity cloning makes an identity-counting rule follow one
  source with accuracy `3/4` and increases expected transfers from `9/128` to
  `15/128`. Its remaining increment must jointly add truthful forecast
  elicitation, endogenous independent-source acquisition, and imperfect
  provenance beyond existing false-name, limited-verification, and
  provenance-cluster mechanisms.

Both cards honestly label overlap risk `high`. The bounded review did not
systematically cover proper scoring, peer prediction, information-acquisition
incentives, prediction markets, market microstructure, cryptographic
attestation, or a full citation graph. Neither direction should proceed from
this contaminated run.

## Windows long-path friction

Two ordinary `framing_team.open` attempts failed without mutation because the
atomic operational target was 274 characters while Windows reported
`LongPathsEnabled=0`. Replaying the same logical request with the standard
`\\?\` spelling for the same physical project root succeeded. Project id,
route run, WorkPacket, envelope, head, and all subsequent bindings remained
unchanged.

This recovery preserves the evidence chain, so it is not a protocol
corruption. It is nevertheless a concrete local-use defect: an ordinary user
should not need to diagnose or rewrite a Windows project path before the first
team operation.

The earlier LocalAppData sandbox approval is recorded separately as host
environment friction. It does not explain or excuse the independent project-
local long-path failure.

## Stop-boundary facts

- research-worker handoffs: **0**
- applied user turns: **0**
- framing candidate commits: **0**
- completion requests: **0**
- post-initialization canonical head changes: **0**
- human gates / G1 decisions: **0**
- later route commits: **0**
- `finish` requests: **0**

The project contains its ordinary genesis transaction. “Zero canonical
writes” is accurate only for route/candidate work after initialization, not
for creation of the project identity.

## Disposition

Keep this run as **protocol PASS, scientific/UX MIXED, input fidelity FAIL**.
Do not choose A or B and do not continue its worker path.

The next implementation slice is limited to:

1. exact UTF-8 brief verification before and after delivery;
2. scientifically neutral project/lane context and explicit separation of
   host capability names from research content;
3. a researcher choice screen that cannot collapse to hashes and a report
   link; and
4. ordinary Windows long-path-safe operational publication.

A later fresh pilot must use a new, shorter neutral root and a neutral project
name. It may validate only the repaired framing-choice experience; it must not
change V8, canonical scientific schemas, routes, gates, or the disabled
ResearchMove batch.
