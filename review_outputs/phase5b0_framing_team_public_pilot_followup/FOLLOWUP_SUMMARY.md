# Phase 5B.0 Scheme-B follow-up evidence

## Archive role

This directory is an additive sibling to the frozen
`phase5b0_framing_team_public_pilot` archive. It records the bounded
nondegeneracy probe, the researcher's Scheme-B choice, the resulting local
repair/decomposition/audit route evidence, the later authorized outcome-locus
continuation, and isolated developer diagnostics.
No byte in the original frozen archive is replaced by this follow-up.

The original archive is bound here by its `evidence_manifest.json` SHA-256:
`32abfbacb73f2b9f2d24093c0aa1a7de9f7c6f4551b37770215825869689a95c`.

## Scientific decision and result

The frozen source-isolated probe returned `PARK` with confidence `0.95`
because the then-current framing did not specify the final review rule and
used `frontier` for an outcome-vector object without a feasible-set,
capacity, objective, or optimization structure.

The researcher subsequently authorized Scheme B only:

- the final rule is score-blind, `phi(s,r) = psi(r)`;
- it is strictly increasing in the review signal, `psi(1) > psi(0)`; and
- the minimal deterministic baseline is `phi(s,r) = r`.

At that checkpoint, the choice did not authorize a human G1 decision, a welfare
ranking, a capacity mechanism, or a change to the ResearchQuestion's
`frontier` terminology.

The follow-up route sequence in `C:\tmp\p5b1` reached these canonical heads:

1. `reframe.repair` disposed the unwanted framing run and opened the exact
   `repair.dependency` successor. The preserved disposition SHA-256 is
   `de98464e0f1811a2c56f91d12bca91c6612c7da9eb9349079d0821e0fdf6c163`.
2. The Scheme-B PrimitiveGraph repair committed at
   `2a61a652135f2b2fcf798b8309fbd158bc8c5e6fba304fe40e93b3829fd28203`.
3. The dependent primitive decomposition refresh committed at
   `23ff15b0b542e21b638071a6f36f6afb2f19197476103874df6a0621bfd716bc`.
4. The first audit candidate was rejected because its replacement G1 dossier
   did not preserve and strengthen the source package. A corrected audit
   candidate then committed at the final head
   `aea3e7a77ab7a3dc0e4d0b334403eb6d9df38a69bf3ddbca730075e6728fd230`.

The committed V8 audit accepts Scheme B's positive active-margin and exact
appeal-to-outcome consequence binding. Its scientific disposition remains
`revise_framing`: the ResearchQuestion still calls an outcome-vector
comparison or locus a strict `frontier` without the structure needed for that
term. The audit records `distinctive_mechanism_contribution_status` as
`not_claimed`. No human G1 decision was requested, confirmed, or fabricated.

## Researcher-authorized outcome-locus continuation

The researcher later made a separate exact choice: replace the
ResearchQuestion's strict `frontier` with `outcome-vector comparison/locus` and
introduce neither capacity nor optimization. The valid ResearchQuestion and
BenchmarkSet repairs committed at `d5665bf4...` and `ed2371db...`.

The first continuation in `C:\tmp\p5b1` is not an admissible final-code
canonical result. It canonically recorded a no-output failed decomposition at
`17fc2667...` while a temporary validator change exempted failed Phase-2
transactions from normal output and exit semantics. That global relaxation was
found to violate the frozen Phase-2 contract and was reverted. Consequently,
the later `04d893e1...` and `4aed9a21...` heads on that lineage cannot replay
under the final strict validator. The subsequent V8 audit also ended
`failed_no_effect` with no audit commit. This lineage is preserved only as
negative developer evidence.

A separate recovery root,
`C:\tmp\p5b1-outcome-locus-clean-20260723`, resumed from the last strict-
replayable head `ed2371db...`. Unreachable experimental run and operational
directories were removed only from that copy; the original `p5b1` evidence was
not changed. Under the restored Phase-2 validator, the clean route sequence is:

1. PrimitiveGraph dependency repair committed at
   `922c2cb43e90b5cadfb79fe6cafc095588df098e78d03324af9b3831bb5d5397`.
2. PrimitiveGraph/G1-dossier decomposition refresh committed after one exact
   changed-facet repair at
   `d99a9aee75130eaeb41c9015fe148afd8976a0ae7a217c77b9dcacc5fd9ed0c4`.
3. Fresh unchanged-V8 audit run
   `run_op_71e7b5655c3610e83c7e8b740f3334c41a7b9b76bde92c52`, WorkPacket
   `bea5202e70485891e9de94cd71df4206e42a7b09f8652a427c092289e0c76447`,
   committed on its first completion at final head
   `bf4e7fdd49bcf089b18318e77075514cbaea027939049113d1d5840073b4800c`.

The final audit uses only allowed information, institution, and timing nodes for
its public-state class; binds an active posterior-threshold appeal witness to
the institutional outcome vector; records
`distinctive_mechanism_contribution_status=not_claimed`; and proposes
`ready_for_g1`. Its replacement dossier proposes `approve`, but neither object
records an effective human G1 decision. Final strict replay reports 12
transactions, 19 entities, 24 relations, and `valid=true` at the exact final
head.

## Evidence boundary

- The nondegeneracy probe was source-isolated and noncanonical. It used no
  engine, network, subagent, directory-external scientific input, or canonical
  write.
- The route candidates in this follow-up were developer-assisted and
  nonblind. They are not fresh WorkPacket-only model evidence and do not prove
  provider-independent delivery, multi-agent benefit, low user effort, or
  research quality.
- The clean recovery is also developer-assisted and nonblind. Its audit draft
  was mechanically rebound from the earlier same-case draft and checked against
  the new exact WorkPacket; it is not a fresh blind or WorkPacket-only model
  evaluation.
- In particular, the decomposition helper carried current canonical objects
  that the delivered WorkPacket omitted; the audit correction followed a
  rejected attempt and source inspection. Those facts must not be relabeled as
  blind model performance.
- `isolated_diagnostics/` comes from a separate developer-test root. Its one
  local decomposition commit is not a commit to the `C:\tmp\p5b1` pilot root;
  the rejected diagnostic attempts are not represented as canonical pilot
  commits.
- The observable model label is only
  `user-selected-current-model-unverified`. The provider/backend identity was
  not independently confirmed.
- The executable follow-up ran from a dirty local source checkout whose base
  Git HEAD was
  `3774d0a3698e61ebfb237d222441bff95b715f9b`. No exact wheel digest or frozen
  dirty-tree snapshot was captured, so the evidence has only a partial source
  binding. `SOURCE_BINDING.json` states this limitation mechanically.
- Crash-recovery and exact-retry code hardening performed after these captures
  is post-pilot work. This run did not exercise it, and this archive makes no
  regression-pass claim for it.
- `OUTCOME_LOCUS_SOURCE_BINDING.json` separately binds the later clean recovery.
  It used the repository's dirty editable source at Git HEAD `56b7342...`, with
  no wheel digest; its source identity is therefore partial and must not be
  conflated with the earlier Scheme-B checkpoint binding.

## Inventory and verification

`evidence_manifest.json` inventories every evidence file in this sibling
archive except itself and its sidecar. `evidence_manifest.sha256` contains the
SHA-256 of the manifest. The manifest declares those two structural exclusions
and the exact expected directory membership, so an extra, missing, or changed
file is detectable without creating a self-referential hash.
