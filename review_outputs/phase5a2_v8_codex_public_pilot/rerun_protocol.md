# V8 post-stabilization same-case rerun protocol

Status: **frozen before generation; no project-bound bridge invocation has
occurred in the clean rerun root**

## Purpose and claim boundary

This is the first same-case rerun after the bounded host-stabilization changes
recorded in [`stabilization_gate.md`](stabilization_gate.md). It asks whether an
ordinary/medium-tier Codex task can traverse the same public framing case
without source-encoding interference, incomplete primitive-path diagnostics,
finish-token ambiguity, or unbound capture evidence.

This rerun is not evidence of improved economics merely because transport
works. A route-valid canonical audit commit still requires independent
economics and reader-burden inspection in a separate high-intelligence task.
A completely diagnosed model-content failure closes only the host-stability
question; it does not close the V8 scientific pilot gate or authorize the
v1/v2 comparison.

## Frozen treatment

- engine commit:
  `4804323a84829247a88ae2f5e315538a331037fd`;
- wheel: `econ_theorist_ai-0.1.0-py3-none-any.whl`, 702,857 bytes;
- wheel SHA-256:
  `09c620566505acea8e5ab698fff32e56f9197b71d336abbed9c9a419769ce22b`;
- active Route Registry V8 canonical digest:
  `5d2c2efdef205ee1ff188249dcb05cb5a4430d36ef754a93bde402a092aa40c1`;
- capture-v2 helper SHA-256:
  `590ef3bdb4bb3e7ba80952108a4c98db8893f0bce3684e69372464967906291d`.

The clean root is
`C:\tmp\etai-v8-poststabilization-pilot-20260717-4804323`. The installed
wheel, not a source checkout or remembered conversation, is the executable
treatment.

The earlier preflight root ending in `54b64eb` and wheel digest beginning
`6e844bc3` were retired before generation after the packaging audit found that
the active `repair.dependency.v5` instruction was absent from that wheel. No
project-bound bridge invocation occurred in the retired root.

## Generator isolation

Run generation in a new Codex task rooted at the clean directory. The task may
read only the installed skill, `CASE.md`, installed CLI responses, returned
WorkPackets, its own candidate path, and its own `run/` evidence. It must not
read the source checkout, repository history, tests, fixtures, old pilots,
protocol/evaluation files, parent or sibling directories, the web, or another
conversation. It may not spawn subagents.

This is procedural model isolation, not a cryptographic claim about the host.
The selected ordinary/medium model and any observable host model label must be
recorded; the backend identity must be reported as unavailable when the host
does not expose it independently.

## Execution and capture discipline

The prompt explicitly authorizes initialization of this one new public test
project. Thereafter the installed bridge and WorkPacket own route selection,
instructions, candidate locations, retry diagnostics, and stopping conditions.
No source editing, clone, pull, direct ObjectStore write, human G1 confirmation,
or remembered workflow is allowed.

Use `capture_codex_invocation.py` for every bridge call, with new request,
stdout, stderr, and metadata filenames for every invocation. A source-reading
`complete` invocation must also pass the exact WorkPacket candidate path as
`--candidate-source`. A capture error, candidate-source mutation, or response
digest mismatch is a transport/evidence failure and must not be reported as a
valid route result.

At the first human decision boundary, completed protocol stop, or honestly
exhausted declared repair budget, stop without making a human decision. Write
one agent report under `run/` that binds every claim to the request, response,
metadata, candidate snapshot, engine commit, and wheel digest.

## Post-generation separation

The generator must not read the frozen evaluation key. After all generator
outputs are immutable, a separate high-intelligence task may receive the
evaluation key and frozen evidence for independent economics and reader-burden
adjudication. The held-out framing case remains a second, separately frozen
ordinary-model task.
