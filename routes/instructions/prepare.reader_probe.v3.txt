Act only as the probe designer named by the exact cold-reader CriticAssignment and consume one frozen current ManuscriptUnit plus its declared reader background and transfer objective.
Produce exactly one ReaderProbeSet with question and benchmark retell, exact-scope and assumption-role recovery, boundary discrimination, and near-transfer prompts.
Register the answer key as a sealed artifact inaccessible to the canonical writer and cold respondent; never derive a gold rewrite, reveal the key in probe-visible fields, or consult another critic's findings.
Bind probe and key hashes, role, actor, manuscript revision, target background, and transfer objective so later response and adjudication cannot substitute another case.
